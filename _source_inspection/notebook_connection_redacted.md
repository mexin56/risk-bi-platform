# 数据库连接示例.ipynb

单元格数：2

## 连接方式（已脱敏）
导入：IPython.core.interactiveshell.InteractiveShell、base64、collections.Counter、datetime、datetime.date,datetime,timedelta、hashlib、hmac、json、numpy、odps.ODPS、odps.models.Schema,Column,Partition、odps.options、os、pandas、requests、sqlalchemy.create_engine、time、urllib.parse、warnings
非敏感变量：ast_node_interactivity、df、exe_time、max_columns、o、reader、settings、sql
凭据变量（仅名称，值未输出）：未识别
Endpoint：未识别
- Code cell 1: ODPS(...)，位置参数=3，关键字参数=endpoint
- Code cell 1: 调用 execute_sql(...)
SQL 中识别的表：api.enable、groupby.position、hive.compatible、holo.table、maxcompute.aliyun、odps.sql、odps.table、pb_biz_credit.lwn_ng_cashsx_x_input_param_sample、service.eu、split.hive、submit.mode